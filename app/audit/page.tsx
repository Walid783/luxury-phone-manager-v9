import {redirect} from 'next/navigation';import AppShell from '@/components/AppShell';import {getSession} from '@/lib/auth';import {prisma} from '@/lib/prisma';import {dateFr} from '@/lib/format';import {ShieldCheck} from 'lucide-react'
export default async function Page(){const s=await getSession();if(!s)redirect('/login');if(s.role!=='ADMIN')redirect('/');const rows=await prisma.auditLog.findMany({take:200,orderBy:{createdAt:'desc'},include:{user:true}});return <AppShell><div className="v6-title-row"><div><div className="eyebrow">SÉCURITÉ · V13</div><h1>Journal d’audit</h1><p>Traçabilité des actions sensibles de l’atelier.</p></div></div><section className="card"><div className="table-wrap"><table><thead><tr><th>Date</th><th>Utilisateur</th><th>Action</th><th>Objet</th><th>Détail</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{dateFr(r.createdAt)}</td><td>{r.user?.name||'Système'}</td><td><span className="badge slate">{r.action}</span></td><td>{r.entity}{r.entityId?` · ${r.entityId.slice(0,8)}`:''}</td><td>{r.detail||'—'}</td></tr>)}</tbody></table></div>{!rows.length&&<div className="empty"><ShieldCheck/> Aucun événement d’audit.</div>}</section></AppShell>}
