import {redirect} from 'next/navigation';import Link from 'next/link';import AppShell from '@/components/AppShell';import {getSession} from '@/lib/auth';import {prisma} from '@/lib/prisma';import {eur,dateOnlyFr,paymentLabel} from '@/lib/format';import {HandCoins,ChevronRight} from 'lucide-react'
export default async function Page(){if(!(await getSession()))redirect('/login');const payments=await prisma.payment.findMany({orderBy:{createdAt:'desc'},include:{repair:{include:{client:true}}}});const total=payments.reduce((a,p)=>a+Number(p.amount),0);return <AppShell><div className="v6-title-row"><div><h1>Acomptes & paiements</h1><p>Historique des encaissements liés aux prises en charge.</p></div><div className="metric-pill"><HandCoins size={18}/><span>Total encaissé</span><b>{eur(total)}</b></div></div><section className="card"><div className="table-wrap"><table><thead><tr><th>Date</th><th>Dossier</th><th>Client</th><th>Méthode</th><th>Note</th><th>Montant</th><th></th></tr></thead><tbody>{payments.map(p=><tr key={p.id}><td>{dateOnlyFr(p.createdAt)}</td><td>TKT-{String(p.repair.ticketNo).padStart(5,'0')}</td><td>{p.repair.client.name}</td><td>{paymentLabel[p.method]}</td><td>{p.note||'—'}</td><td><b>{eur(Number(p.amount))}</b></td><td><Link className="icon-link" href={`/repairs/${p.repairId}`}><ChevronRight size={16}/></Link></td></tr>)}</tbody></table></div>{!payments.length&&<div className="empty">Aucun paiement enregistré.</div>}</section></AppShell>}
