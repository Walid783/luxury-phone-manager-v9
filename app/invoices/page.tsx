import { redirect } from 'next/navigation';import Link from 'next/link';import AppShell from '@/components/AppShell';import { getSession } from '@/lib/auth';import { prisma } from '@/lib/prisma';import { eur,dateFr } from '@/lib/format';import { FileText,ChevronRight } from 'lucide-react'
export default async function Invoices(){if(!(await getSession()))redirect('/login');const rows=await prisma.invoice.findMany({orderBy:{issuedAt:'desc'},include:{repair:{include:{client:true,payments:true}}}});return <AppShell><div className="hero-head compact"><div><div className="eyebrow">COMPTABILITÉ</div><h1>Factures</h1><p>Factures générées depuis les dossiers atelier.</p></div></div><div className="card"><div className="table-wrap"><table><thead><tr><th>Facture</th><th>Date</th><th>Client</th><th>Dossier</th><th>Montant</th><th>État</th><th></th></tr></thead><tbody>{rows.map(i=><tr key={i.id}><td><b>F-{i.invoiceNo}</b></td><td>{dateFr(i.issuedAt)}</td><td>{i.repair.client.name}</td><td>#{i.repair.ticketNo}</td><td>{eur(Number(i.repair.total))}</td><td><span className={`badge ${i.paid?'green':'amber'}`}>{i.paid?'Payée':'À payer'}</span></td><td><Link className="circle-link" href={`/invoices/${i.id}`}><ChevronRight size={18}/></Link></td></tr>)}</tbody></table></div>{!rows.length&&<div className="empty"><FileText size={28}/>Aucune facture. Ouvrez un dossier réparation pour en générer une.</div>}</div></AppShell>}
