import { NextResponse } from 'next/server';import { destroySession,getSession } from '@/lib/auth';import {audit} from '@/lib/audit';export async function POST(req:Request){const s=await getSession();if(s)await audit({userId:s.id,action:'LOGOUT',entity:'User',entityId:s.id,detail:'Déconnexion atelier'});await destroySession();return NextResponse.redirect(new URL('/login',req.url),303)}
