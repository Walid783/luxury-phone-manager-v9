import {redirect} from 'next/navigation';import AppShell from '@/components/AppShell';import {getSession} from '@/lib/auth';import {prisma} from '@/lib/prisma';import {ShoppingCart,AlertTriangle} from 'lucide-react'
export default async function Page(){if(!(await getSession()))redirect('/login');const items=await prisma.inventoryItem.findMany({where:{quantity:{lte:1}},orderBy:{quantity:'asc'}});return <AppShell><div className="v6-title-row"><div><h1>À commander</h1><p>Liste d'achat automatique basée sur les niveaux de stock.</p></div><div className="metric-pill"><ShoppingCart size={18}/><span>Références à traiter</span><b>{items.length}</b></div></div><section className="card"><div className="table-wrap"><table><thead><tr><th>Référence</th><th>Pièce</th><th>Catégorie</th><th>Stock</th><th>Seuil</th><th>Suggestion</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.sku}</td><td><b>{i.name}</b></td><td>{i.category}</td><td><span className="badge red">{i.quantity}</span></td><td>{i.minQuantity}</td><td><span className="plan-pill"><AlertTriangle size={13}/> Commander {Math.max(1,i.minQuantity*2-i.quantity)}</span></td></tr>)}</tbody></table></div>{!items.length&&<div className="empty">Aucune commande urgente.</div>}</section></AppShell>}
