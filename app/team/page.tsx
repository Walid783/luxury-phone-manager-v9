import { redirect } from 'next/navigation';import AppShell from '@/components/AppShell';import { getSession } from '@/lib/auth';import { prisma } from '@/lib/prisma';import { Users,Plus,ShieldCheck } from 'lucide-react';import { dateFr } from '@/lib/format'
export default async function Team(){const s=await getSession();if(!s)redirect('/login');if(s.role!=='ADMIN')redirect('/');const users=await prisma.user.findMany({orderBy:{createdAt:'desc'},include:{_count:{select:{repairs:true}}}});return <AppShell><div className="hero-head compact"><div><div className="eyebrow">ÉQUIPE</div><h1>Techniciens & accès</h1><p>Gérez les comptes atelier et les responsabilités.</p></div></div><div className="detail-grid"><section className="card"><div className="section-title"><div><h2><Users size={19}/> Équipe active</h2><p>{users.length} compte(s)</p></div></div><div className="table-wrap"><table><thead><tr><th>Nom</th><th>Email</th><th>Rôle</th><th>Dossiers</th><th>Créé</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td><b>{u.name}</b><small className="cell-sub">{u.active?'Actif':'Inactif'}</small></td><td>{u.email}</td><td><span className={`badge ${u.role==='ADMIN'?'amber':'blue'}`}>{u.role}</span></td><td>{u._count.repairs}</td><td>{dateFr(u.createdAt)}</td></tr>)}</tbody></table></div></section><aside className="card"><div className="section-title"><div><h2><Plus size={19}/> Ajouter un compte</h2><p>Mot de passe initial modifiable plus tard.</p></div></div><form className="mini-form" action="/api/team" method="post"><input name="name" placeholder="Nom du technicien" required/><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Mot de passe initial" minLength={4} required/><select name="role" defaultValue="TECHNICIAN"><option value="TECHNICIAN">Technicien</option><option value="ADMIN">Administrateur</option></select><button className="btn"><ShieldCheck size={16}/> Créer le compte</button></form></aside></div></AppShell>}
