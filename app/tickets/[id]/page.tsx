import { notFound } from 'next/navigation';import QRCode from 'qrcode';import { prisma } from '@/lib/prisma';import { dateFr,statusLabel } from '@/lib/format'
export default async function Ticket({params}:{params:Promise<{id:string}>}){const {id}=await params;const r=await prisma.repair.findUnique({where:{id},include:{client:true}});if(!r)notFound();const base=process.env.NEXT_PUBLIC_APP_URL||'http://localhost:3000';const track=`${base}/track/${r.trackingToken}`;const qr=await QRCode.toDataURL(track,{margin:1,width:180});return <div className="ticket-page"><div className="ticket"><header><div><div className="ticket-logo">LUXURY <span>PHONE</span></div><small>Centre de réparation · Atelier Pro</small></div><div className="ticket-no">DOSSIER <b>#{r.ticketNo}</b></div></header><section className="ticket-grid"><div><small>CLIENT</small><b>{r.client.name}</b><span>{r.client.phone}</span></div><div><small>APPAREIL</small><b>{r.deviceBrand} {r.deviceModel}</b><span>{r.imei||'IMEI non renseigné'}</span></div><div><small>DÉPÔT</small><b>{dateFr(r.createdAt)}</b><span>{r.accessories||'Aucun accessoire indiqué'}</span></div><div><small>STATUT</small><b>{statusLabel[r.status]}</b><span>{r.conditionIn||'État non renseigné'}</span></div></section><section className="ticket-issue"><small>PANNE / DEMANDE</small><p>{r.issue}</p></section><section className="ticket-track"><img src={qr} alt="QR suivi"/><div><b>Suivi client</b><p>Scannez ce QR code pour consulter l’avancement de la réparation.</p><small>Référence #{r.ticketNo}</small></div></section><footer>Merci pour votre confiance · Conservez ce ticket jusqu’à la restitution de l’appareil.</footer></div><div className="btn no-print">Imprimez avec Ctrl + P</div></div>}
