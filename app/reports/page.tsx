import { redirect } from 'next/navigation';import AppShell from '@/components/AppShell';import { getSession } from '@/lib/auth';import { prisma } from '@/lib/prisma';import { eur } from '@/lib/format';import { BarChart3,TrendingUp,Wallet,Wrench,Users } from 'lucide-react'
export default async function Reports(){if(!(await getSession()))redirect('/login');const now=new Date();const month=new Date(now.getFullYear(),now.getMonth(),1);const [revenue,repairs,clients,paid]=await Promise.all([prisma.payment.aggregate({where:{createdAt:{gte:month}},_sum:{amount:true}}),prisma.repair.count({where:{createdAt:{gte:month}}}),prisma.client.count(),prisma.repair.count({where:{status:'DELIVERED',updatedAt:{gte:month}}})]);return <AppShell><div className="page-heading"><div><div className="eyebrow">PILOTAGE</div><h1>Rapports & performances</h1><p>Les indicateurs essentiels de votre atelier en un coup d'œil.</p></div></div><div className="grid kpi-grid"><div className="card kpi-card"><span className="kpi-icon"><Wallet/></span><div><small>CA encaissé ce mois</small><strong>{eur(Number(revenue._sum.amount||0))}</strong><em>Paiements enregistrés</em></div></div><div className="card kpi-card"><span className="kpi-icon"><Wrench/></span><div><small>Dossiers créés</small><strong>{repairs}</strong><em>Depuis le 1er du mois</em></div></div><div className="card kpi-card"><span className="kpi-icon"><TrendingUp/></span><div><small>Réparations livrées</small><strong>{paid}</strong><em>Ce mois-ci</em></div></div><div className="card kpi-card"><span className="kpi-icon"><Users/></span><div><small>Base clients</small><strong>{clients}</strong><em>Clients enregistrés</em></div></div></div><div className="card gap-top report-placeholder"><BarChart3 size={34}/><h2>Centre de reporting V5</h2><p>Les données sont déjà reliées à PostgreSQL. Cette zone pourra accueillir les graphiques CA, panier moyen, délais de réparation, marge pièces et performance techniciens.</p></div></AppShell>}
